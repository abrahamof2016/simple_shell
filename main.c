#include "shell.h"
/**
  * main - simple basic shell.
  * @ac: an argument count.
  * @av: an array of pointers to char.
  * @envp: an array of pointers to char.
  * Return: an integer value.
  */
int main(void)
{
	prompt();
	return (0);
}
